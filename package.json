{
  "name": "wd-cli",
  "version": "1.3.0",
  "private": true,
  "description": "Workspace Director - fast project navigation CLI",
  "type": "module",
  "scripts": {
    "dev": "bun run src/index.ts",
    "build": "bun build src/index.ts --compile --outfile dist/wd-bin",
    "test": "bun test src/",
    "test:integration": "bun test integration/template-integration.test.ts"
  },
  "dependencies": {
    "@inquirer/prompts": "^7.0.0",
    "commander": "^13.0.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.7.0"
  }
}